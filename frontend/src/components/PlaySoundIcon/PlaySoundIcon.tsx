"use client";

import { Volume2 } from "lucide-react";

const PlaySoundIcon = () => {
  return (
    <Volume2 size={64} strokeWidth={1.5} className="hover:text-whiteDimmed" />
  );
};

export default PlaySoundIcon;
